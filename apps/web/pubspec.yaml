name: aegivue
description: Aegivue camera and recording client.
publish_to: none
version: 0.1.0+1
environment:
  sdk: ^3.8.0
dependencies:
  dio: ^5.11.0
  flutter: {sdk: flutter}
  flutter_web_plugins: {sdk: flutter}
  go_router: 16.2.1
  provider: ^6.1.5+1
  web: ^1.1.0
dev_dependencies:
  flutter_test: {sdk: flutter}
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/
