name: vigilo_web
description: Vigilo camera and recording client.
publish_to: none
version: 0.1.0+1
environment:
  sdk: ^3.8.0
dependencies:
  dio: ^5.11.0
  flutter: {sdk: flutter}
  web: ^1.1.0
dev_dependencies:
  flutter_test: {sdk: flutter}
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
